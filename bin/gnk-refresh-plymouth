#!/bin/bash

if [[ "$1" == "-y" ]] || gum confirm "Refresh Plymouth theme? This will replace your current startup screen with gnk defaults."; then
  sudo cp ~/.local/share/gnk/default/plymouth/* /usr/share/plymouth/themes/gnk/
  sudo plymouth-set-default-theme -R gnk
fi
